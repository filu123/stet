# components/ui

Shared, dumb, stateless primitives (Button, IconButton, Tooltip, …).

Rules: no business logic, no store access, no data fetching. Props in, pixels out.
Styled exclusively with design tokens from `globals.css`.

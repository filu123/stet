# feature: settings (v0.2 — not yet implemented)

User settings: AI provider + API key (localStorage only, never sent anywhere except
the provider's API), model choice, proactive/on-demand mode, aggressiveness level.

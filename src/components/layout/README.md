# components/layout

The app shell: `Sidebar`, `TopBar` (breadcrumbs), `AppShell`.
Composes features; contains no feature logic itself.
(`DocumentCard` lives in `components/ui/` — it's a dumb primitive.)
